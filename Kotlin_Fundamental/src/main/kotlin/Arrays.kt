fun main(){
    val mixArray = arrayOf(30," Januari ", 1993,"SMK Telkom Purwokerto", true)
    print(mixArray[3])
    println(" berdiri pada tanggal "+(mixArray[0]) + (mixArray[1]) + (mixArray[2])+".")
    println("That's "+(mixArray[4]) +" bro.")
}

